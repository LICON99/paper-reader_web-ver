# paper-reader_web-ver

논문 읽기 편한 프로그램입니다.

PDF 논문을 읽으면서 **드래그 번역**과 **논문 질문 채팅**을 쓸 수 있는 로컬 웹 앱.
모든 AI 호출은 이 PC에 설치된 Claude Code CLI(`claude`)를 통해 처리된다. 별도 API 키 불필요.

macOS 전용이다.

## 필요한 것

- macOS + Python 3.9 이상 (`python3 --version`으로 확인)
- [Claude Code](https://claude.com/claude-code) 설치 및 **로그인** — 서버가 `claude` 실행 파일을 자동으로 찾는다
- `pdftotext` — PDF 가져오기에 필요하다. 없으면 설치:

  ```bash
  brew install poppler
  ```

## 실행

`start.command` 더블클릭 (또는 터미널에서 `./start.command`).
브라우저가 `http://127.0.0.1:8765/`로 자동으로 열린다.
터미널 창은 읽는 동안 켜 둔다. 닫으면 서버가 종료된다.

처음 받은 직후 `start.command`가 실행되지 않으면 실행 권한을 준다:

```bash
chmod +x start.command
```

처음 실행하면 불러온 논문이 없다. 화면의 **PDF 가져오기**(또는 상단 `논문 ▾` 메뉴)로
읽을 PDF를 선택하면 텍스트 추출까지 끝난 뒤 바로 읽을 수 있다.
가져온 논문과 대화 기록은 `data/`에 로컬로만 저장되며 저장소에 올라가지 않는다.

## 기능

| 기능 | 사용법 |
|---|---|
| 드래그 번역 | 본문 텍스트를 드래그 → 팝업에서 `번역` 클릭. 모델은 **Opus 고정**(자연스러운 한국어 우선, 영어 병기는 문단당 1~2개로 제한), 같은 문장은 캐시되어 즉시 표시 |
| 번역 보기 | 상단 `번역` 버튼 — 사이드바가 **현재 읽는 페이지의 번역본**으로 바뀌고, 스크롤을 따라온다. 본문에서 문장을 드래그하면 번역본에서 **그 문장**이 노랗게 표시된다(문장 단위 정렬 저장). 표는 마크다운 표로 복원되고 수식·첨자는 LaTeX 표기로 렌더링된다. 번역본이 없으면 `전체 번역 시작`으로 준비한다(PDF 가져오기 직후엔 자동 시작). 문장 위에 마우스를 올리면 원문이 뜬다 |
| 질문에 첨부 | 드래그 → `질문에 첨부` → 선택 부분이 인용된 채로 채팅 질문 |
| 논문 채팅 | 상단 `채팅` 버튼(기본). 논문 전문이 매 질문에 함께 전달되며, 답변의 `[p.N]`을 누르면 해당 페이지로 이동 |
| 모델·생각 깊이 선택 | 채팅 입력창 **아래** 드롭다운 2개. 모델: Sonnet/Opus/Haiku/Fable, 생각 깊이: 가볍게/보통/깊게/아주 깊게/최대. 선택값은 브라우저에 저장되어 다음 방문에도 유지 |
| 세션 | 질문을 보내면 자동으로 저장된다. 사이드바 `세션 ▾`에서 목록을 열면 기본은 **현재 논문의 세션만** 보이고, `[전체]` 토글로 모든 논문의 세션을 볼 수 있다(선택은 기억된다). 다른 논문의 세션을 클릭하면 그 논문으로 자동 전환. `×`로 삭제. 서버를 껐다 켜도 유지된다 |
| 새 대화 | `새 대화` 버튼 — 새 세션 시작(이전 세션은 목록에 남음) |
| PDF 가져오기 | 상단 `논문 ▾` → `PDF 가져오기…` → 파일 선택. 텍스트 추출까지 자동으로 끝나면 그 논문으로 전환되고, 전체 페이지 번역이 백그라운드로 시작된다 |
| 논문 삭제 | `논문 ▾` 목록에서 `×` — PDF·추출 텍스트·번역본이 지워진다. 대화 기록은 남는다 |

## 구조

```
paper-reader/
  server.py        로컬 HTTP 서버 (Python 표준 라이브러리만)
  prompts.json     번역·채팅 프롬프트 (한국어, UTF-8)
  start.command    실행 진입점
  web/index.html   리더 UI (pdf.js 렌더링 + 번역 팝업 + 채팅)
  vendor/          pdf.js 4.10.38 + KaTeX 0.18.4 (로컬 사본, 오프라인 동작)
  data/
    papers/<id>/   논문 라이브러리: paper.pdf + paper.txt(페이지 마커)
                   + meta.json(제목) + trans.json(페이지별 번역, 생기면)
    sessions/      세션별 대화 기록 (<key>.json)
    active.json    현재 열려 있는 논문 id
```

`data/` 레이아웃은 이전 PowerShell 판과 같다. 예전에 쓰던 `data/` 폴더를 그대로 복사해 오면
논문과 대화 기록이 이어진다.

- 번역: `claude -p --model opus --tools ""` 단발 호출, stdin으로 프롬프트 전달.
- 채팅: CLI `--resume`이 print 모드에서 맥락을 잇지 못해, 서버가 대화 이력을 보관하고
  매 턴 논문 전문 + 이력(최근 16문답)을 함께 보낸다. 이력 전체는 디스크에 저장된다.
- PDF 가져오기: 업로드 → pdftotext로 텍스트 추출 → `===== [p.N] =====` 페이지 마커 부여
  → `data/papers/p<타임스탬프>/`에 저장 후 활성화. pdftotext가 없으면 메뉴에 비활성으로 표시된다.
- 전체 번역: 백그라운드 작업이 페이지 단위로 claude(Opus)를 호출해 `trans.json`에
  문단→문장 정렬(`{src, ko, sents:[{src,ko}]}`)로 저장한다. 문장 단위라 드래그
  하이라이트가 정확하고, 표/수식은 마크다운 표·LaTeX(`$...$`)로 받아 클라이언트가
  렌더링한다. 페이지마다 저장되므로 중간에 끊겨도 이어서 하고, 초반 3페이지가 연속
  실패하면(대개 로그인 문제) 나머지를 시도하지 않고 멈춘다. 논문 분량만큼 Opus 호출이
  나가므로 사용량 한도를 그만큼 소모한다.
- 동시 요청은 `ThreadingHTTPServer`가 처리한다.
- 옵션: `python3 server.py --port 9000 --no-browser`
